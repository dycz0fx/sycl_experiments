GPU - CPU Bufffer
=================

Experiments to pass data from GPU to CPU

Build Instructions
------------------
```
$ mkdir build
$ cd build
$ cmake ..
$ cmake --build .
$ ctest
```

To run tests manually:
```
$ srun ./src/EXE
```

